const frinds=["ghada", "malak" ,"ghaida"];

frinds.forEach(function(frind){
    console.log("I have a friend named " + frind);
});


// -----------------------------------------------------
const numbers = [1, 5, 8];
const numbersTimesTwo = numbers.map(function(num) {
  return num * 2;
  
});
const timesTwo = numbers.map(num => num * 2);
console.log(numbers)


//------------------------------------------------------------


const instructors = ["Alex", "Daniel", "Morgan", "Ben", "Micah", "Jims"];
const tt=instructors.map(function(instructors){
    console.logt(tt+"is awesome")  

}
